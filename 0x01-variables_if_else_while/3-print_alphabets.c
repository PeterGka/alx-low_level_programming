#include <stdio.h>
/**
 * main - The main entry point for the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++);

	for (ch = 'A'; ch <= 'Z'; ch++);
	{
		putchar('\n');
	}
	return (0);
}
