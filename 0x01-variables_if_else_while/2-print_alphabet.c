#include <stdio.h>
/**
 * main - The main entry point for the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char lc;
	for lc = 'a'; lc <= 'z';
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
