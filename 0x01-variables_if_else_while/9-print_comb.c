#include <stdio.h>
/**
 * main - The main entry point for the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		if (n != 57)

			putchar(n);
	}

		{
			putchar(',');
			putchar(' ');

		}
		return (0);
}
