#include "main.h"

/**
 * main - The main entry point for the program
 *
 * Return: Always 0 (Success)
 */

void print_alphabet(void)
{
	char la;

	for (la = 'a'; la <= 'z'; la++)

		_putchar(la);

	_putchar('\n');
}
